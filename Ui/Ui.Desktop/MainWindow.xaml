<Window x:Class="RietRob.Mofeto.Ui.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="{Binding MainWindowTitle}" Height="450" Width="800" 
        DataContext="{Binding Main, Mode=OneWay, Source={StaticResource Locator}}">

    <!-- Example of how to bind the Message Listener into the View 
    <Window.IsEnabled>
        <Binding Path="BindableProperty" Source="{StaticResource MessageListener}" />
    </Window.IsEnabled>-->
    <Grid>
        <!-- Example for how to bind a button to a command -->
        <!--<Button Content="SayHello" Command="{Binding SayHelloCommand, Mode=OneWay}" />-->
    </Grid>
</Window>
